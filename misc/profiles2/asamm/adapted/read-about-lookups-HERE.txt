Make sure lookups.dat file is distributed to all places where needed.
Lines must never be deleted, changes are incremental.
There are major and minor version numbers at the start of the file. Keep them updated.

